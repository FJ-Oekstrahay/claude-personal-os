---
name: Session 2026-04-11 — Low-touch session
description: Model router, before/after diff table, safety mitigations, Safety Officer + HoG reviews
type: project
originSessionId: 75734cf9-e980-455c-8296-ecf9aff60c51
---
## What was built

**Model router (`cli/ai.py`):**
- `_classify(config, prompt)` — cheap Haiku call classifies complexity before each `ask()`
- JARFACE tier escalates to Sonnet on complex requests, shows `⚡ JARFACE` signal
- `classify_only=True` flag skips server KV rate limit increment (no double-billing)
- Classifier uses 10s timeout vs. 60s for main calls

**Before/after diff (`cli/agent.py`):**
- `_read_before(fc, commands)` reads current param values before applying
- `_verify_changes()` updated: "Was → Now" table replacing mislabeled "Before/After"
- Table title changed to "Changes Applied"

**Safety mitigations (`cli/agent.py`):**
- `_SAFETY_CRITICAL_PARAMS` dict — failsafe/arming/motor_protocol params require secondary confirmation
- `_PARAM_LIMITS` dict — bounds for PID P/I/D/F values; out-of-range triggers red warning + second confirm
- `_check_safety(commands)` — scans proposed commands before touching FC
- try/finally on BF apply cycle — bridge.refresh() guaranteed even if apply throws
- Undo path: bridge.refresh() added after restore; message clarifies GUI-only settings aren't restored
- In-loop save: pre-flight reminder added after successful flash write
- Exit-time save: styled Panel replaces plain prompt; pre-flight reminder added
- Agent system prompt: explicit safety constraints on P>100, D>70, failsafe_*, arming params, motor_protocol, adjrange channel selection

**Reviews completed:**
- `SAFETY_REVIEW.md` — Safety Officer review. 3 BLOCKING, 2 SHOULD FIX, 2 MONITOR. All 5 minimum mitigations implemented.
- `HOG_REVIEW.md` — Head of Growth review. Post will land strong (600-1200 upvotes predicted). 3 fixes before posting: add screenshot, compress fair warnings, clarify radio feature.

**Usage.py fixes:**
- JARFACE tier caps: 200/day, 2000/month (was using free caps of 3/day)
- Device ID cached — `system_profiler` only runs once per process
- Tier-aware rejection messages (JARFACE users no longer told to "upgrade to JARFACE")

## What remains before launch (Geoff-blocked)

1. Hardware validation — full agent session on real FC
2. BF GUI hardware test — `--bf-gui` on live FC
3. Proxy deploy — `proxy/DEPLOY.md`, needs Cloudflare account + dedicated Anthropic key
4. License tokens wired into CLI
5. Repo private
6. Three launch post fixes (screenshot, fair-warning compression, radio feature clarification)
7. Pricing provisional answer for launch post
