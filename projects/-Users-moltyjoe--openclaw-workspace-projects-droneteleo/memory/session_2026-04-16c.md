---
name: Session 2026-04-16c
description: Testing gap diagnosis, tracking/inventory product expansion, meta-strategy discussion
type: project
originSessionId: 5df9e0ff-849f-479d-9e93-3f120e911f31
---
## Testing gap diagnosis

JARFACE failed its first live interactive `dt agent` test — "claimed it can't talk to FC."

Root cause identified: `run_jarface_test.py` hardcodes `--dry-run` (line 73). The apply path
(FC reconnect at line 791 in agent.py, `_apply_commands`, `_verify_changes`) was never exercised
by any automated test. Eval harness uses FAKE_FC_CONTEXT; scripted bench test forces dry-run.

Two fixes written as prompt 17:
1. Add `--live` flag to bench test runner + `jarface_live_turns.json`
2. Add `refuses_to_act.yaml` eval case — fail if JARFACE produces prose-only response to a simple change request

Transcript logging gap also identified: interactive sessions produce no transcript. Added to
prompt 17 as Gap 3. When debugging, check `logs/session_<timestamp>_<fc_name>.txt` once implemented.

Prompt 18 written: how to paste a transcript into a new session for debugging.

## New product areas

Seymour research confirmed: no hobbyist FPV tool tracks battery cycles, craft hours, or maintenance.
Research saved to `knowledge/research/fpv-tracking-inventory.md`.

New prompts written:
- 19: Pack & craft tracking — voltage-sag trending, craft hours, pack identity (barcode/QR/manual)
- 20: Build advisor / combination explorer — motor+prop+frame+FC compatibility advisor
- 21: Prop & maintenance tracking — photo-based prop inspection, blackbox vibration baseline, bearing calendar

Business plan updated:
- Phase 3b added: pack/craft/maintenance tracking
- Phase 5 build advisor formalized

## Flying intensity metric

RMS throttle from blackbox = good proxy for "how hard was this session."
A 10-min freestyle session punishes a pack much more than 10-min cruising at same duration.
Computable from existing blackbox infrastructure.

## CTO and Gadfly involvement needed

Before building tracking (prompts 19-21): CTO data model review (Flight as central entity,
schema for pack/craft/component health, cloud sync model).

After CTO: Gadfly review of pack tracking concept (is manual entry too annoying? what's the
minimum entry point that builds habit?).

Safety Officer: before shipping any "don't fly this pack" advisory language.
