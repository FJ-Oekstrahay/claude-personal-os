---
name: Session 2026-04-19c
description: Force-update/kill-switch design discussed; eval --judge guidance; parallel agent windows explained; eval_cmd.py --help improved
type: project
originSessionId: 43a30f02-65d6-4a2b-9a2c-323ed5e623ce
---
## What shipped

### cli/eval_cmd.py — improved --help
- Docstring fully rewritten: `--judge` semantics, `--all + --judge` compatibility, per-model cost guidance, "never run harness directly" warning
- Argparse help strings tightened for `--judge`, `--case`, `--group`, `--all`
- **Status: code written, not yet committed**

## Decisions / answers captured

### Force-update / kill-switch (not yet implemented)
- No server-side version gate exists today. Rotating JARFACE proxy token boots all users globally.
- Design decided: version floor via `/v1/status` endpoint (D1 lookup on startup), per-token suspend flag, `dt admin suspend <token>` command.
- Infrastructure: Cloudflare Worker + D1 `clients` table already viable; ~2-3h implementation.
- ToS language: updates mandatory for security floor bumps; minor versions optional.
- **Blocked: needs dedicated implementation session.** Prompt written in `prompts-and-questions/force-update-kill-switch.md`.

### Parallel agent windows
- Visual subagent panels are a VS Code extension / Claude desktop app UI feature only.
- CLI (terminal) users don't see them; subagent dispatch still works identically.
- No token efficiency gap — purely cosmetic difference.
- If Geoff wants the visual: install Claude Code VS Code extension.

### dt eval --judge usage rule
- No `--judge`: structural assertions only. Use for iteration (fast, free).
- `--judge`: LLM semantic scoring. Use before AGENT_SYSTEM merges + release gates.
- `--judge --all`: compatible. Cost ~30-40 judge calls on full suite.
- Default model: haiku. Release gate: `--model sonnet`.

## Remaining blockers (carried from prior session)
- CTO session on AGENT_SYSTEM proxy path ownership (multi-turn architecture)
- Gadfly review blocking build advisor (prompt 20)
- Force-update/kill-switch implementation (new — prompt available)
- Pack/prop tracking (prompts 19+21): spec APPROVED, waiting on compound-knowledge write-back stability
