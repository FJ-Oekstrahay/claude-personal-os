---
name: Session 2026-04-19b
description: Batch skill fixed; 4 slam dunks shipped: readline, rate-feel, batch code-dump rule, needs_pwr tag; blockers documented
type: project
originSessionId: 96b80a04-7a54-4d6a-ad14-1ba300d089e2
---
## What shipped

### batch.md fixed
- Added explicit "never dump code inline" rule — all code edits route to Cob, only summary reported to main context
- Added 4th bucket: "Cob subagent (code)" to classification list
- Fixed $ARGUMENTS handling guidance: items must be in the SAME message as `/batch`
- Updated playbook `batch_skill_model_invocation_constraint.md` with usage rules

### agent.py
- `import readline` added (try/except for portability) — fixes arrow keys, command history, cursor movement in `dt agent`
- Rate-feel translation added to AGENT_SYSTEM: ACTUAL rates °/s mapped to feel labels (smooth/moderate/aggressive/race); BETAFLIGHT rates explained as rc_rate+roll_rate+expo combination

### tests/eval_harness.py
- `needs_pwr` tag added to `--tags` help string (was already supported by harness, just undocumented)

## Root cause of batch skill issue
The `/batch` slash command fails when typed ALONE as a standalone message — `$ARGUMENTS` is empty and "preceding user message" is just "/batch". Fix: items must follow `/batch` in the same message. The disable-model-invocation error only occurs in harness/eval contexts, not standalone sessions.

## Slam dunk blockers (do NOT attempt without resolving)

### CTO session required before coding:
- **Multi-turn architecture** — switching subsequent turns from `ai.ask(full_prompt)` to `ai.ask_messages(messages)`. Currently, only the FIRST turn uses proper multi-turn API; subsequent turns serialize the whole conversation as a blob. Fix is in agent.py:1258. Blocker: CTO must decide if bundled-tier proxy users should receive full AGENT_SYSTEM (340 lines, ~2k tokens/turn cost increase). Prompt is in `prompts-and-questions/CTO-agent-system-ownership.md`.
- **AGENT_SYSTEM proxy path** — same CTO session.

### Gadfly required before coding:
- **Build advisor (prompt 20)** — spec is DRAFT, Gadfly review blocking.

### Waiting on write-back stability:
- **Pack/prop tracking (prompts 19+21)** — spec APPROVED, not started. Waiting for compound-knowledge write-back to stabilize across more sessions.

## State of previously-listed slam dunks
- adjrange JSON parser fix — DONE (brace-counting extractor, already shipped)
- DTR save bug — DONE (fc_pending stays open after apply, already shipped)
- Compound learning write-back — DONE (session 2026-04-18b)
- capture/learn commands — DONE (session 2026-04-19)
- RPM filter + rates skills — DONE (session 2026-04-19)
- Multi-turn eval harness — DONE (session 2026-04-19)
