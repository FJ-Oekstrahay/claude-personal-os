---
name: Session 2026-04-16
description: Eval harness baseline run — 10/15 passing with judge; system prompt fixes for confirmation gate and trailing questions
type: project
originSessionId: eada3ef1-1b0a-4a84-8f21-8a84dea1762c
---
# Session 2026-04-16

## What was done

**10 missing eval cases written** (`tests/cases/`):
adjrange_query, propwash_filter, blackbox_informed, battery_cell_context, rate_profile_query,
personality_no_ai_disclaimer, compare_builds, gyro_lpf1_zero_flag, undo_flow, save_flow

**Eval harness baseline: 10/15 (67%) with judge**

**System prompt fixes (cli/agent.py AGENT_SYSTEM):**
1. Removed "Ask for confirmation" from workflow step 4 — it contradicted the safety section and caused trailing "Confirm to apply?" on every response
2. Added: "Do not end responses with questions like 'Want to adjust anything else?'"
3. Added save-request handler: when pilot says "save it", tell them to type 'save' to the CLI

**Harness fix (tests/eval_harness.py):**
- Added per-case `no_compress: true` field — bypasses `_compress_fc_context` for cases with non-k=v data (adjrange labels survive)

## Consistent JARFACE failures (known issues, not test case problems)

| Case | Root cause |
|---|---|
| `battery_cell_context` | Misses dshot_idle_value + throttle_limit_percent for voltage change |
| `pid_query` | Adds unsolicited filter info to simple PID query |
| `propwash_filter` | Recommends wrong levers (gyro/P instead of dterm + notch) |
| `rate_profile_query` | Context blindness — doesn't cite roll_rate=70 specifically |

Stochastic (sometimes pass): `save_flow`, `compare_builds`

## Why 10/15 is the Haiku floor

These 4 consistent failures are Haiku attention limits + knowledge gaps. To push past this
would need targeted system prompt additions for:
- 6S voltage change → specific BF params to check
- Propwash diagnosis → dterm_lpf1_dyn_max and dyn_notch_q as first levers

Or upgrade to Sonnet for evals.

## Key gotcha discovered

Running harness with system `python3` (no pyyaml) silently uses fallback YAML parser that
doesn't handle `|` block scalars — `fc_context` loads as literal `'|'`.
Always run: `.venv/bin/python tests/eval_harness.py --judge`
