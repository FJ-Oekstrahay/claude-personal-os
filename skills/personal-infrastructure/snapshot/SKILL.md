Look at the conversation so far and write a snapshot file.

1. Pick a short, descriptive kebab-case name that captures what we've been working on (e.g. "droneteleo-serial-cli", "sales-pipeline-debug"). No prompt — just pick.

2. Get the current datetime with: `date +%Y-%m-%d-%H%M`

3. Write the file to `~/.openclaw/workspace/HANDOFF-{name}-{datetime}.md` using this structure:

## What We're Doing
One or two sentences on the current task/goal.

## Current State
Where things stand right now — what's working, what's not, what's in progress.

## Key Files / Paths
Relevant file paths, commands, or config locations that the next session would need.

## Next Step
One concrete action to pick up from here.

4. Confirm: "Snapshot saved: HANDOFF-{name}-{datetime}.md" and stop. No summaries, no board updates, no agent spawns.
